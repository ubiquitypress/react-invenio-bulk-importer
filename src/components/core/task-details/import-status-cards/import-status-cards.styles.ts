import { css } from '@/styled-system/css';

export const styles = {
  statistic: css({
    marginBlock: '1rem'
  }),
  progressLabel: css({
    marginBlockEnd: '0.5rem'
  }),
  progress: css({
    marginBlockEnd: '0.5rem'
  })
};
