import { css } from '@/styled-system/css';

export const styles = {
  fileInput: css({
    marginBlockStart: '0.5rem'
  }),
  helpText: css({
    marginBlockStart: '0.5rem',
    fontSize: '0.75rem',
    color: '#666'
  }),
  buttonContainer: css({
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'flex-end',
    gap: '0.5rem'
  })
};
